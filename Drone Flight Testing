### Start Gazebo and RViz

First, open a new terminal.

Then run the following commands to start Gazebo and RViz:

```bash
cd ~/ardu_ws
source install/setup.bash
ros2 launch ardupilot_gz_bringup iris_runway.launch.py
```
